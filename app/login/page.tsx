'use client'
import {FormEvent,useState} from 'react'
import {signIn} from 'next-auth/react'
import {useRouter} from 'next/navigation'
export default function Login(){const router=useRouter();const [register,setRegister]=useState(false);const [name,setName]=useState('');const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError('');try{if(register){const r=await fetch('/api/auth/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,email,password})});const d=await r.json();if(!r.ok)throw new Error(d.error);setRegister(false)}else{const r=await signIn('credentials',{email,password,redirect:false});if(!r?.ok)throw new Error('Invalid email or password');router.push('/')}}catch(e){setError(e instanceof Error?e.message:'Something went wrong')}finally{setLoading(false)}}return <main className="flex min-h-screen items-center justify-center px-4"><div className="glass w-full max-w-md rounded-3xl p-7 shadow-soft"><div className="mb-8"><div className="text-xl font-bold">PATCHBAY<span className="text-orange-500">.</span></div><h1 className="mt-8 text-3xl font-bold">{register?'Create your account':'Welcome back'}</h1><p className="mt-2 text-sm text-neutral-500">{register?'Start building with your AI providers.':'Sign in to continue building.'}</p></div><form onSubmit={submit} className="space-y-4">{register&&<input value={name} onChange={e=>setName(e.target.value)} required minLength={2} placeholder="Name" className="w-full rounded-xl border bg-transparent px-4 py-3 outline-none"/>}<input value={email} onChange={e=>setEmail(e.target.value)} required type="email" placeholder="Email" className="w-full rounded-xl border bg-transparent px-4 py-3 outline-none"/><input value={password} onChange={e=>setPassword(e.target.value)} required minLength={8} type="password" placeholder="Password (8+ characters)" className="w-full rounded-xl border bg-transparent px-4 py-3 outline-none"/>{error&&<div className="rounded-xl bg-red-50 px-4 py-3 text-sm text-red-700">{error}</div>}<button disabled={loading} className="w-full rounded-xl bg-neutral-900 py-3 font-semibold text-white disabled:opacity-50 dark:bg-white dark:text-black">{loading?'Please wait…':register?'Create account':'Sign in'}</button></form><button onClick={()=>setRegister(!register)} className="mt-5 w-full text-sm text-neutral-500 hover:text-neutral-900 dark:hover:text-white">{register?'Already have an account? Sign in':'Need an account? Create one'}</button></div></main>}
