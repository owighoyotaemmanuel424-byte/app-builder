import {auth} from '@/auth'
import {db} from '@/lib/db'
import Link from 'next/link'
import {redirect} from 'next/navigation'
export default async function Projects(){const session=await auth();if(!session?.user?.id)redirect('/login');const projects=await db.project.findMany({where:{userId:session.user.id},orderBy:{updatedAt:'desc'}});return <main className="min-h-screen px-4 py-6 md:px-8"><div className="mx-auto max-w-6xl"><header className="flex items-center justify-between"><Link href="/" className="text-xl font-bold">PATCHBAY<span className="text-orange-500">.</span></Link><Link href="/" className="rounded-xl bg-neutral-900 px-4 py-2 text-sm font-semibold text-white dark:bg-white dark:text-black">New build</Link></header><div className="mt-12"><h1 className="text-3xl font-bold">Projects</h1><p className="mt-2 text-sm text-neutral-500">Your generated applications.</p></div>{projects.length===0?<div className="mt-10 rounded-3xl border p-10 text-center text-sm text-neutral-500">No projects yet. Start your first build.</div>:<div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{projects.map(p=><Link href={`/builder/${p.id}`} key={p.id} className="glass rounded-2xl p-5 transition hover:-translate-y-0.5 hover:shadow-soft"><div className="flex items-center justify-between"><span className="font-semibold">{p.name}</span><span className="text-xs text-neutral-500">{p.status}</span></div><p className="mt-2 line-clamp-3 text-sm text-neutral-500">{p.description}</p><div className="mt-6 text-xs text-neutral-400">Edited {p.updatedAt.toLocaleDateString()}</div></Link>)}</div>}</div></main>}
